# Supabase project URL（公开值）
SUPABASE_URL=https://YOUR-PROJECT.supabase.co

# Supabase anon key（前端公开使用，不要填 service_role）
SUPABASE_ANON_KEY=YOUR-ANON-KEY

# Edge Function 名称（部署到 ${SUPABASE_URL}/functions/v1/${SUPABASE_EDGE_FUNCTION}）
SUPABASE_EDGE_FUNCTION=request-lut-download

# 投稿 LUT Edge Function 名称（仅登录用户投稿 .cube）
SUPABASE_SUBMIT_LUT_FUNCTION=submit-lut

# 管理员审批 Edge Function 名称（仅 admin 角色）
SUPABASE_MODERATE_FUNCTION=moderate-submission

# Cloudflare Turnstile site key（前端公开使用，必须以 0x4 开头）
TURNSTILE_SITE_KEY=0x4AAAAAAA-EXAMPLE-PLACEHOLDER

# 本文件可提交。复制为 .env 并填写真实值：
#   cp .env.example .env
# .env 已在 .gitignore 中被忽略。
